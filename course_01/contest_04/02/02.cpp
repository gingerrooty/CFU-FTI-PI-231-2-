#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    double a;
    double b;
    double c;
    std::cin >> a >> b;
    c = (sqrt(a * a + b * b));
    std::cout << c;
    return 0;
}
